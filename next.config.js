/** @type {import('next').NextConfig} */
const nextConfig = {
    // This is the updated configuration
    serverExternalPackages: ['better-sqlite3'],
    devIndicators: false

    // Keep any other configuration options you might have
}

module.exports = nextConfig
